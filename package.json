{
  "name": "drkingbd-cc",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler kv:namespace create SUGGESTIONS && wrangler types && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lint": "npx tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@types/telegram-web-app": "^9.6.0",
    "@grammyjs/types": "^4.0.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.102.0"
  },
  "dependencies": {
    "@grammyjs/auto-retry": "^2.0.2",
    "grammy": "^1.45.1",
    "hono": "^4.12.31"
  }
}
